# Validation report — 2026-08-25

Live Ollama (bge-m3:latest embeddings from committed cache, gemma3:12b chat), 3 runs over the 384-page / 28-day fixture.

## Thresholds (spec §7.4)

| Check | Threshold | Measured | Pass |
|---|---|---|---|
| Clustering accuracy (ARI vs ground truth) | ≥ 0.75 | 0.996 (runs: 0.996, 0.996, 0.996) | yes |
| Exclusion precision (uncategorized really ambiguous/junk) | ≥ 0.70 | 1.000 | yes |
| Run-to-run stability (pairwise ARI) | ≥ 0.85 | 1.000 (pairs: 1.000, 1.000, 1.000) | yes |

## Clustering detail

- Scored on the **200 topical pages the run actually ingested**, of 324 topical pages in the fixture. The 28-day visit schedule never browses the remainder, so they have no topic to compare against.
- Pairwise precision: 1.000, pairwise recall: 0.993 (means over 3 runs)
- Exclusion recall (of truly ambiguous/junk pages, share excluded): 0.817
- Uncategorized row counts per run: 13, 13, 13

## Adjudication confusion (all 0 audited clusters across runs)

Ground truth definition: keep = one truth topic; split = ≥ 2 truth topics with ≥ 2 pages; uncategorized = majority ambiguous/junk.

```json
{}
```

Disagreement-style exclusions of clean clusters (rate): 0.000

## Sequential registry identity (28 runs)

All checks passed: single topicId for the dormant topic across 28 runs, dormant event on fixture day 21 (spec §4.7 crossing = lastActiveDay + 14; §7.4's "day 22" was an off-by-one in the spec), revival on day 25, convergence on day 8, gap days 15–17 reported, day 18 lowData.

## Verdict

All Phase 3 acceptance thresholds met.
